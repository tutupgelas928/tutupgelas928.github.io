source "https://rubygems.org"

gem 'wdm', '>= 0.1.0' if Gem.win_platform?

# Faraday retry middleware (menghilangkan warning)
gem "faraday-retry"

group :jekyll_plugins do
  gem 'kramdown'
  gem 'rouge'
end
